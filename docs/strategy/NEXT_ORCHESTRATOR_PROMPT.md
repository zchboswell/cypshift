# Next orchestrator kickoff — integrate D-142, then run the sole official attempt

This handoff becomes active on the D-142 aggregate-acceptance packaging branch.
Signed D-141 commit `61e335485ab983cdf1f030aedefd64ea8252b492` is
integrated; PR CI run `33112350539` and exact-SHA post-main CI run
`33113306698` are green. The sole fixed no-argument G2-7H composite acceptance
ran exactly once from clean synchronized `main`, exited zero, cleaned its fixed
work roots, and published one immutable public acceptance receipt. Review,
sign, integrate, and require green exact-SHA post-main CI for D-142 before any
official preflight or execution.

The objective remains the smallest scientifically defensible path to a
materially better OpenADMET CYP 2026 submission. Frozen family-safe evidence
outranks leaderboard evidence. Fixed MapLight remains the best validated
system at internal component-macro MAE `0.5837812652`. D-142 is composite-
orchestration mechanics evidence, not model-quality evidence.

## Restore authoritative context first

Read and follow `AGENTS.md`, then read completely in order:

1. `docs/strategy/PROJECT_STATE.md`
2. `docs/phases/README.md`
3. `docs/phases/PHASE_2_OPENADMET_GLOBAL_V2.md`
4. `docs/strategy/PROJECT_CHARTER.md`
5. D-122 through D-142 in `docs/strategy/DECISIONS.md`
6. the final relevant rows of `runs/experiment_ledger.csv`
7. the exact G2-7G contract and tracked public claim; D-135 acceptance; D-136
   bridge; D-137 repair contract; D-138 seal erratum; D-139 and D-140
   transition contracts; D-141 four-file implementation; and D-142 receipt

Treat clean synchronized `main`, immutable receipts, exact-SHA CI, and exact
fixed-root existence checks as authoritative. If the official one-use attempt
or any partial/staging root already exists, or the tracked claim appears
changed or consumed, stop; do not repair, repeat, resume, move, or replace it.
Never inspect, import, copy, execute, patch, or derive implementation from the
rejected G2-7B runner/driver. D-127/D-128 remain permanently barred.

## Exact accepted lineage

- corrected execution contract:
  `9464b0947255298a8de8836af6178857841bb2a55bc5c0f4897be2ba91151bcf`
- tracked immutable public claim:
  `d7e68837a9df0b392eab7d03282ec84d21b8787f4b2ac14b1fc79fec44df6f9f`
- scientific runner:
  `dca9b8d1be51a29fa4e2269949d1f3339ecf14d99b91f203aa2cacdd2ca90bde`
- D-135 formal science-kernel acceptance:
  `4c886d0dd51bfb48095ac2a8f88b202e78cb85f840f8f7bd474c2982ffedf390`
- D-136 provenance bridge:
  `2820c30f387d138d115b36f621b038dc75a1f5af43a7fa9f97b3b837a33a0dc3`
- D-137 repair contract:
  `f6576d61147731066dd09577338ab236b5ee0054eb4380377fa3bf6f0534b967`
- D-138 seal-order erratum:
  `a3e1bd653f28297357380ad14da3fcd640d89d3476954830c8fd63c2f3faeb33`
- D-139 test-transition contract:
  `6703ad308d5a4188e5b42aa325cf59d9d10729e08ba0ed2c0dce44d445709c2c`
- D-140 source-shape transition contract:
  `d4ff0e57b4c5d8b6bae808d0749f5b8e116965f18f2df3fee6e04e58dd727417`
- D-141 corrected official driver:
  `feab960a54dd5ff818e29d062ad8eba48538658fe38a75d01f7c76f3d2daf103`
- D-141 fixed composite acceptance driver:
  `3e209b88df7634f47884ce45653673a5407310575146392a77811fb4ed67ba9f`
- D-141 focused tests:
  `f17b5b2f39b92892b046f289d6ebdb1888d705ea7a27ea24b3ca3013d39289b0`
- D-141 exact six-marker conftest:
  `03d92bf3a2890a61190a6a4fc7a6bc59fa900ed6ea4b904223b1f2f991699d95`
- D-141 signed integrated commit:
  `61e335485ab983cdf1f030aedefd64ea8252b492`
- D-141 PR and post-main CI:
  `33112350539` and `33113306698` (`success`)
- D-142 composite-orchestration acceptance receipt:
  `92a18f0e6837d70d4bb39560d42a22cfb23acac8ea72a955b9656b392d954596`
- D-142 standalone receipt audit test:
  `10ebb8f18d38f6d069e35d3994468e6a70dc0de3df6cb5736352721be439a28c`

The D-142 receipt is 72,449 canonical JSON bytes with status
`G2_7H_MAPLIGHT_ROBUSTNESS_OFFICIAL_ORCHESTRATION_ACCEPTED`. It was published
as a regular, single-link `0444` file. Git does not preserve read-only bits, so
the mode is publication evidence; canonical bytes and SHA-256 are the portable
CI identity.

## D-142 accepted mechanics evidence

The immutable receipt proves:

- forward and reverse orders with the same six scenarios each;
- 12 scenario and 12 supervisor invocations;
- byte-identical normalized result maps across order;
- all five official terminal statuses plus fail-closed pre-consumption
  propagation;
- all eleven frozen mechanics flags and ten once-only probe counters;
- one selection token and no runner-up where selection is legal;
- zero science under the underpowered status;
- strict aggregate-only terminal lineage and authority;
- complete per-scenario and pre-publication cleanup; and
- zero retained private roots and no rejection record.

D-135's 3,480 model-double invocations, 667,872 synthetic predictions, and two
real CatBoost controls are inherited with `reexecuted=false`. D-142 performed
none of them. Every live D-135-through-D-141 implementation, contract, and
immutable science hash matches the receipt.
The standalone receipt audit passes `1/1`; it binds canonical byte identity,
live lineage, opposite-order map identity, mechanics, cleanup, privacy, and
zero authority without asserting the non-portable checkout mode.

Every top-level forbidden-operation counter is zero: no official source or
baseline byte, official/tracked/private claim creation or consumption, model
double, real CatBoost fit, prediction, official metric, confirmatory/test/TDI
row, submission, portal selection, or upload operation occurred. The receipt
separately records the mechanics it did exercise in disposable public roots:
10 scenario fixture-claim publications, six probe publications, and one
intentionally interrupted synthetic staging. Those are not tracked, private,
or official claim operations. The tracked template remains byte-identical,
unusable, and unconsumed. D-142 grants zero official-execution, model-quality,
confirmatory, submission, or upload authority.

## Exact next gate

Make one atomic signed D-142 receipt-audit-and-knowledgebase commit, push the
branch, open a PR, require all checks, integrate locally by fast-forward only,
push `main`, and require green exact-SHA post-main CI. Do not invoke the
official driver from the D-142 branch.

Only after D-142 is green on clean synchronized `main`, perform a fresh
read-only official preflight proving all of the following without opening an
official source or baseline byte:

- D-122 contract and D-135-through-D-142 receipt hashes are exact;
- the D-141 official driver and immutable science-kernel hashes are exact;
- the tracked public claim is byte-identical, unusable, and unconsumed;
- fixed official attempt, restricted, publication-staging, final-terminal,
  and claim-staging roots are absent by exact non-following checks;
- no official-driver process is active;
- Python 3.12.3/RDKit 2026.3.5 and the symlinked Python 3.10.13/NumPy
  1.25.2/CatBoost 1.2.1 model runtime authenticate;
- the frozen 7.68 wall-hour, 128 CPU-core-hour, 51.2 GB storage, 15.36 GiB RSS,
  and zero-GPU envelope is available;
- cumulative supervisor and cleanup boundaries are ready; and
- official source/baseline allowlist and receipt identities authenticate
  without opening their data bytes.

If and only if every preflight item is green, invoke the integrated official
driver exactly once with no root or output arguments. Do not retry, resume,
move, repair, overwrite, replace, run a smaller battery, alter order, add a
cache, change concurrency, or promote a runner-up. Do not open any official
source or baseline byte in the outer process; only the supervised child may do
so after atomically consuming the sole private claim.

Monitor the accepted 720-or-1,020-fit battery through its immutable terminal.
Interpret only the contracted aggregate status and package it as a separate
reviewed milestone. A retained full MapLight result is scientifically valid if
no deletion qualifies. Never use diagnostics to revise the one selection
token.

## Closed capabilities

Until the official aggregate is complete, reviewed, integrated, and green:

- do not access confirmatory truth, historical row-level artifacts, blinded
  test, TDI, external acquisition, submission, official metric, leaderboard,
  private portal, or upload capabilities;
- do not inspect official source/baseline bytes outside the authorized
  supervised child;
- do not create a second claim, retry, or reinterpret a one-use gate; and
- do not add candidates, features, seeds, groups, retries, calibration,
  blends, caches, concurrency, frameworks, or services.

EXP-G3 and G1/G2/M1/X1/T2 plus immutable R5D/I0 remain closed;
`global_TDI` remains only the TDI fallback.
