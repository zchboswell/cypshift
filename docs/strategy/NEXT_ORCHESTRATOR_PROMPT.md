# Next orchestrator kickoff — G2-7G sole official execution gate

This handoff becomes active only after D-135 is integrated on `main` through
the repository's signed fast-forward-only workflow and post-main CI is green
for that exact commit. Until then, review and integrate only the D-135
aggregate acceptance milestone. Do not run the official driver from its
milestone branch.

The objective remains the smallest scientifically defensible path to a
materially better OpenADMET CYP 2026 submission. Frozen family-safe evidence
outranks leaderboard evidence. Fixed MapLight remains the best validated
system at internal component-macro MAE `0.5837812652` (`0.5838`); D-135 is
synthetic execution-mechanics evidence and makes no model-quality claim.

## Restore authoritative context first

Read and follow `AGENTS.md`, then read in order:

1. `docs/strategy/PROJECT_STATE.md`
2. `docs/phases/README.md`
3. `docs/phases/PHASE_2_OPENADMET_GLOBAL_V2.md`
4. `docs/strategy/PROJECT_CHARTER.md`
5. D-122 through D-135 in `docs/strategy/DECISIONS.md`
6. the final relevant rows of `runs/experiment_ledger.csv`
7. the exact G2-7G contract, claim, runner, official driver, accepted formal
   receipt, and focused tests

Treat clean synchronized `main`, immutable receipts, CI, and exact fixed-root
existence as authoritative. If a one-use attempt already exists or the claim
appears consumed, partial, or changed, stop; do not repeat or repair it. The
rejected G2-7B implementation and the D-127/D-128 claim, attempt, and roots
remain permanently barred and may not be opened, imported, copied, executed,
patched, benchmarked, or used to derive code.

## Accepted implementation and formal evidence

- execution contract:
  `9464b0947255298a8de8836af6178857841bb2a55bc5c0f4897be2ba91151bcf`
- tracked immutable claim:
  `d7e68837a9df0b392eab7d03282ec84d21b8787f4b2ac14b1fc79fec44df6f9f`
- scientific runner:
  `dca9b8d1be51a29fa4e2269949d1f3339ecf14d99b91f203aa2cacdd2ca90bde`
- official one-use driver:
  `1675336e449ba9a8327406cb37f82f08e3547076ce6c69fa0ade70c5a3de57fc`
- formal acceptance driver:
  `7cb471ce6c39e4633b91556cd2c09ee7406dd39912b5d69200fad9372a42e473`
- at-attempt D-134 focused tests:
  `3fedd87eb86f485167a53564cb440409056d82982f329db888028e294228c53f`
- post-attempt state-transition tests:
  `625f3ae0c8d61dc76775240606a2662d7b9b80b5ffe0c05344b9a587f0c6ca2b`
- aggregate receipt-audit tests:
  `80c08d89eabfcfb17d73ea19f833ee46005f0bac0421b2a1bb239191e7fca97e`
- sole formal acceptance receipt:
  `4c886d0dd51bfb48095ac2a8f88b202e78cb85f840f8f7bd474c2982ffedf390`

The formal status is `G2_7G_MAPLIGHT_ROBUSTNESS_EXECUTION_ACCEPTED`. Two
fresh roots reversed physical and fit order. Each ran exact M0/full retention
at 720 model-double fits and deterministic M2/drop-Avalon at 1,020, for 3,480
model-double invocations and 667,872 synthetic predictions total. Capability
and aggregate-terminal maps were byte-identical. Exactly two bounded real
CatBoost controls completed. Cumulative supervision observed 6,985 checkpoints
and 13 descendants over 187.62508345600054 wall-seconds and
381.12050561399997 CPU-seconds, with peak RSS 435,781,632 bytes and peak
storage 104,341,504 bytes. Cleanup was complete, with zero warnings, GPU,
detached children, official operations, claim creation or consumption, or
retained private roots.

## Fresh preflight before the irreversible attempt

After D-135 integration and exact-SHA green post-main CI, prove all of the
following without opening, listing, or hashing official source or baseline
bytes:

- clean synchronized signed `main` and the exact accepted parent/receipt
  hashes above;
- the tracked claim template is unchanged, unusable, unconsumed, and still has
  exactly five null future receipts;
- the fixed official attempt and restricted roots, every official terminal,
  and every partial or forbidden replacement root are absent;
- the tracked source and baseline allowlist receipt metadata match the
  contract, without opening, listing, or hashing the referenced official
  bytes;
- the trusted compiler/scorer runtime is Python 3.12.3 and the model runtime
  is Python 3.10.13, NumPy 1.25.2, and CatBoost 1.2.1;
- cumulative supervision and cleanup predicates are ready;
- free disk and the fixed CPU, wall, RSS, storage, network, and zero-GPU
  boundaries are available; and
- no other agent has advanced or partially created this one-use gate.

The supervised child must begin cumulative supervision and consume the sole
private claim before it opens any official source or baseline byte. The
tracked claim never mutates.

## Invoke exactly once

From clean synchronized `main`, with no root or output override, run exactly:

```text
uv run --python 3.12.3 python research/maplight-fixed/run_global_v2_maplight_robustness_official_v2.py
```

Do not retry, resume, repair, move, overwrite, replace, reorder, shrink, or run
an alternate battery. Do not interrupt a healthy execution.

The immutable battery is:

- Stage A: 540 fits;
- Stage B: 180 fits;
- conditional Stage C: 300 fits only if a deletion is selected;
- total: exactly 720 or 1,020 fits and 562,752–797,232 predictions;
- exactly one selection token and no runner-up; and
- limits of 7.68 wall-hours, 128 CPU-core-hours, 51.2 GB restricted storage,
  15.36 GiB simultaneous RSS, and zero GPU.

Monitor only aggregate progress and resource evidence. Never expose row-level
private values. The only valid terminal interpretations are:

- `G2_7_PRIMARY_CONTENDER_FROZEN`
- `G2_7_MAPLIGHT_ROBUSTNESS_UNDERPOWERED`
- `G2_7_MAPLIGHT_ROBUSTNESS_REJECTED`
- `G2_7C_MAPLIGHT_ROBUSTNESS_RESOURCE_ABORTED`
- `G2_7_MAPLIGHT_ROBUSTNESS_FAILED`

One result selects exactly one contender or retains full MapLight under the
frozen D-122 rules. Diagnostics cannot revise the token. A failed gate is
evidence, not authority to promote a runner-up or tune the path.

## After the terminal

Preserve only contracted aggregate evidence and submission-safe receipts;
never commit official structures, targets, row-level predictions, model
caches, or unrestricted logs. Package the official aggregate result as a
separate D-numbered reviewed signed milestone, integrate it locally by
fast-forward only, push `main`, and require green post-main CI before opening
any G2-8 confirmatory implementation or truth.

## Hard boundaries

- Do not access confirmatory truth, historical row-level artifacts, blinded
  test, or TDI data.
- Do not generate or upload a submission or call an official metric.
- Do not use private leaderboard or prior-submission evidence for selection,
  and never publish a private submission identifier, score, or rank.
- Do not add candidates, features, seeds, groups, calibration, blends, clips,
  retries, caches, concurrency, frameworks, or services.
- Do not mutate or reuse the tracked claim, formal receipt, or barred history.

EXP-G3 and G1/G2/M1/X1/T2 plus immutable R5D/I0 remain closed;
`global_TDI` remains only the TDI fallback.
