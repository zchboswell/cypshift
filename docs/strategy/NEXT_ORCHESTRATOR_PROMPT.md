# Continue Phase 3 competition recovery

The user authorized the audited recovery plan, autonomous implementation,
signed milestone commits/pushes, and regular additional entries. The user will
upload manually; notify them when actual files are ready. Aim for 3–4 qualified
releases per week across both tracks, without leaderboard-driven tuning.

Read AGENTS.md, PROJECT_STATE.md, the active Phase 3 plan, PROJECT_CHARTER.md
and D-151 onward in DECISIONS.md. Inspect Git status and preserve user changes.
The full former midnight handoff (including the user's uncommitted edit) is
preserved at ../archive/intake/NEXT_ORCHESTRATOR_PROMPT_2026-08-31.md. It is
historical context, not an active no-repair or competition-withdrawal command.

Execute the next milestone in PROJECT_STATE.md. Finish a coherent runnable
slice, test it, review its diff, sign/commit/push, open a PR, pass checks, then
integrate locally by fast-forward and push main. Never rewrite signed commits.
Use bounded recoverable attempts and cached fits with exact provenance.
Preserve scientific failures, family isolation, raw assay data and the reserved
comparison; do not resurrect TRACE or consume old closed-lane claims.

Keep private artifacts outside Git. Every handoff names a validated private
CSV, track, new model change, OOF evidence, limitations, recommendation, and
whether it replaces a previous recommendation. Do not call a historical hash
check a new entry. Do not automate uploads or infer that an upload occurred.
Keep private portal activity and feedback out of model selection.

Prefer predictive evidence and working release artifacts over new contracts.
A blocked candidate does not block the other candidates or the baseline.
