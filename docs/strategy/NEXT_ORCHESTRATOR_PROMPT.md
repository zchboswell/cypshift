# Next orchestrator kickoff — integrate D-137, then implement G2-7H

This handoff becomes active with the D-137 official-orchestration repair
contract on its contract-only branch. Review and integrate that exact contract
through the repository's signed fast-forward-only workflow, then require green
post-main CI for the exact integrated commit. Do not implement the repair, run
the composite acceptance, or invoke the official driver from the contract
branch.

The objective remains the smallest scientifically defensible path to a
materially better OpenADMET CYP 2026 submission. Frozen family-safe evidence
outranks leaderboard evidence. Fixed MapLight remains the best validated
system at internal component-macro MAE `0.5837812652` (`0.5838`). D-135 is
immutable synthetic science-kernel mechanics evidence; D-136 is provenance
infrastructure evidence; D-137 is contract-only orchestration evidence. None
is a model-quality improvement.

## Restore authoritative context first

Read and follow `AGENTS.md`, then read in order:

1. `docs/strategy/PROJECT_STATE.md`
2. `docs/phases/README.md`
3. `docs/phases/PHASE_2_OPENADMET_GLOBAL_V2.md`
4. `docs/strategy/PROJECT_CHARTER.md`
5. D-122 through D-137 in `docs/strategy/DECISIONS.md`
6. the final relevant rows of `runs/experiment_ledger.csv`
7. the exact G2-7G contract, claim, accepted D-135 receipt, D-136 bridge, and
   D-137 repair contract

Treat clean synchronized `main`, immutable receipts, exact-SHA CI, and fixed-
root existence as authoritative. If a one-use attempt already exists or the
claim appears consumed, partial, or changed, stop; do not repeat or repair it.
The rejected G2-7B implementation and the D-127/D-128 claim, attempt, and roots
remain permanently barred and may not be opened, imported, copied, executed,
patched, benchmarked, or used to derive code.

## Accepted parents and current contract

- corrected execution contract:
  `9464b0947255298a8de8836af6178857841bb2a55bc5c0f4897be2ba91151bcf`
- tracked immutable claim:
  `d7e68837a9df0b392eab7d03282ec84d21b8787f4b2ac14b1fc79fec44df6f9f`
- scientific runner:
  `dca9b8d1be51a29fa4e2269949d1f3339ecf14d99b91f203aa2cacdd2ca90bde`
- historical official driver:
  `1675336e449ba9a8327406cb37f82f08e3547076ce6c69fa0ade70c5a3de57fc`
- D-135 formal acceptance:
  `4c886d0dd51bfb48095ac2a8f88b202e78cb85f840f8f7bd474c2982ffedf390`
- D-136 bridge:
  `2820c30f387d138d115b36f621b038dc75a1f5af43a7fa9f97b3b837a33a0dc3`
- D-136 integrated commit:
  `1c0c5d0f293579a8748e25b3951f9234409bfa39`
- D-136 exact-SHA post-main CI:
  `33090557041` (`success`)
- D-137 repair contract:
  `f6576d61147731066dd09577338ab236b5ee0054eb4380377fa3bf6f0534b967`
- D-137 contract-integrity tests:
  `814675f28c637b30a7d8eea3bf275ee28e39694f1b33127403e95f261490f9c9`

D-135 already proved both opposite-order roots and both conditional profiles,
3,480 model-double fits, 667,872 synthetic predictions, byte-identical
capability and terminal maps, exactly two real CatBoost controls, cumulative
supervision, and complete cleanup. Do not rerun or reinterpret it.

After D-136 integration and green post-main CI, the final pre-consumption audit
rejected the still-unrun official path before supervision. The historical
driver would:

- collapse compiler underpower and ordinary supervised failures into
  `G2_7C_MAPLIGHT_ROBUSTNESS_RESOURCE_ABORTED`;
- discard the structured cumulative observation on supervised failure;
- retain zero-valued allowed official-input accounting after work began; and
- lack accepted atomic claim staging, fixed publication/restricted-root
  cleanup, and symlink-safe cleanup mechanics.

No official attempt or restricted root, private claim, official source or
baseline byte, fit, prediction, metric, confirmatory truth, blinded test,
submission, leaderboard observation, or upload was created, opened, or run.

## Exact next milestone

First review and integrate D-137 exactly, then require green exact-SHA
post-main CI. Only afterward create one new `codex/` implementation branch and
change the following exact surface:

- patch only
  `research/maplight-fixed/run_global_v2_maplight_robustness_official_v2.py`
  in production;
- add the fixed no-argument composite acceptance driver
  `research/maplight-fixed/run_global_v2_maplight_robustness_official_orchestration_acceptance.py`;
- add dedicated focused tests at
  `tests/test_openadmet_global_v2_maplight_robustness_official_orchestration.py`;
- update only the knowledgebase, benchmark index, tests, and ledger needed to
  bind those exact bytes.

The official driver repair must authenticate D-135 as historical science-
kernel evidence, require the future composite acceptance before claim
derivation, make underpowered reachable only from the compiler's exact
exception, classify only the four frozen hard-resource reasons as resource-
aborted, classify every other supervised defect as failed, preserve and
validate cumulative observations from canonical delimiter `; observation=`
and exactly 13 typed fields with no extras, reconstruct truthful aggregate accounting,
stage and fsync the claim at its fixed path before atomic no-replace
publication, use fixed supervisor-owned publication staging, remove the fixed
restricted root, unlink symlinks without following them, and atomically
promote one aggregate-only status-specific terminal without replacement.
Accounting counts authoritative capability crossings, not syscalls or
in-memory reparsing. It separately validates 73,575 group-fold rows, source
central points, finite reported bounds, raw/generated model-feature and fold
rows, generated prediction reopens for scoring, scoring truth, training
targets, baseline rows, and exactly 56 tutorial calls
under the maximum of 80. Each consumed-claim terminal must contain
`attempt_receipt.json`; underpowered also contains aggregate `preflight.json`.
The post-supervision seal may create and fsync the receipt before making the
tree read-only and promoting it, within the frozen byte, wall, CPU, and zero-
new-operation limits. Pre-consumption failure removes claim staging,
restricted state, publication staging, and any empty attempt root, leaves no
claim/receipt/terminal, and halts automation without fabricating an official
status or permitting the fixed claim/driver to run again.

Do not classify a hard-resource reason until its observation passes exact
schema, type, finiteness, nonnegative, return-code, and boolean validation. A
missing or malformed post-claim observation may publish only an accounting-
incomplete failed terminal through the same bounded seal, never resource-
aborted. The same defect before claim consumption follows the no-terminal
cleanup rule.

For scientific, rejected, or underpowered sealing, require return code to be
integer zero, positive checkpoint and descendant counts, complete cleanup and
isolation, zero GPU/detachment/warnings, and every observed resource within the
unchanged parent maxima. Reserve the full five wall-seconds, five CPU-seconds,
16-MiB terminal, and 1-MiB receipt before success; do not widen the 7.68-hour,
128-core-hour, 51.2-GB, 15.36-GiB, zero-GPU envelope. Successful and canonical-
exception outcomes use the same bounded seal. A seal defect permits only one
same-invocation minimal failed or resource-aborted seal. A final-path collision
or failed minimal promotion cleans staging, retains the consumed claim,
publishes no replacement terminal, and blocks reinvocation.

D-137 also freezes a count-label erratum. D-122/D-133 values `562,752` and
`797,232` are unattainable branch upper projections because every non-primary
Stage-B overlay requires positive confirmatory-touch exclusions. The official
prediction count is the exact sum of completed stage manifests, strictly below
the applicable projection. This changes no science, mechanics, or selection.

Do not change the runner, compiler, scorer, wrapper, cumulative supervisor,
MapLight runner, candidates, features, parameters, seeds, groups, fits,
predictions, metrics, thresholds, gates, clips, resources, selection token, or
no-runner-up rule. Do not add retries, resume, cache, concurrency, frameworks,
or services.

Run focused and full checks, package the implementation as one reviewed signed
milestone, integrate by local fast-forward only, push `main`, and require green
post-main CI. Stop there in that milestone: the fixed composite acceptance
must run only afterward from clean synchronized `main` and exactly once. Its
contracted two orders each exercise six scenarios: scientific success, clean
underpower, scientific rejection, hard-wall resource abort, ordinary nonzero
failure, and pre-consumption supervisor failure. The acceptance has eleven frozen
mechanics checks and zero official, claim, model-fit, prediction, or metric
operation.

## Closed capabilities

Until the composite receipt is separately reviewed, integrated, and green on
post-main CI:

- do not invoke the official G2-7G driver;
- do not create, derive, mutate, or consume a private claim;
- do not open, list, hash, or copy official source or baseline bytes;
- do not access confirmatory truth, historical row-level artifacts, blinded
  test, TDI, external acquisition, submission, official metric, leaderboard,
  private portal, or upload capabilities; and
- do not retry, resume, move, overwrite, replace, or reinterpret D-135 or any
  barred attempt.

EXP-G3 and G1/G2/M1/X1/T2 plus immutable R5D/I0 remain closed;
`global_TDI` remains only the TDI fallback.
