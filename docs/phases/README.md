# Active phase

No modeling phase is active. The installable core, public-data baseline, and
representation-comparator work are complete and preserved.

The next phase begins only when an authoritative external evaluation contract
can be frozen without assumptions. At that point, create one concise phase file
here containing the exact data, metric, family-holdout, comparator, and stopping
rules before fitting.

Completed plans are historical evidence and live under
[`docs/archive/phases/`](../archive/phases/).
